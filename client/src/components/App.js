import React from 'react';
import { BrowserRouter, Switch } from 'react-router-dom';

const App = () => (
   <BrowserRouter>
      <Switch>
      </Switch>
   </BrowserRouter>
);

export default App;
